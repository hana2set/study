# INDEX

## INDEX 판단
인덱스를 거쳐 접근하는 오버헤드를 고려해 판단해야함.
- 사용
  - 테이블 행의 수가 많음(**1~2만 건 이상**)
  - 검색 결과가 전체 데이터의 **2~4%** 정도일 때 
  - 해당 컬럼이 많이 사용될 때 (**JOIN, WHERE, ORDER BY 절**)
  - NULL을 포함하는 컬럼이 많을 때
- 비권장
  - 검색결과의 양이 10% 가 넘음
  - DML(쓰기)이 자주 일어나는 경우
  - 중복도가 높음
